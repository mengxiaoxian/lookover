# M0 验收记录（A2 / A3 命中率）

> 前置：重启 DSH Host 后，开一个**新会话**逐条提问。每条问题换一条发送。
> 留痕日志：`~/.dsh/look/log.jsonl`（每次 agent 调用 `look` 追加一行；无新增 = 未调用）。
> 查看调用次数：`wc -l ~/.dsh/look/log.jsonl`，或 `tail ~/.dsh/look/log.jsonl`。

## A1 零权限 ✅（已验）

系统设置未授予任何 TCC 权限，helper 返回完整 app 级信息（2026-XX-XX 实测：钉钉 / bundleId / bounds）。

## A5 不抢焦点 ✅（已验）

`lsappinfo front` 在调用 `look` 前后一致（ASN:0x0-0x54054: 未变）。

## A6 自感知诚实 ✅（代码路径已验）

helper 在「只剩自身窗口」时返回
`{"error":"frontmost_unavailable","note":"当前除 DSH 自身外没有其他可见窗口…"}`，
系统提示要求 agent 如实转述、不编造。（重启后建议实操复验一次。）

## A2 该看就看（核心，需人工跑）

### 坏样本记录（用于迭代文案）

- **2026-09-20「刚才那个网页再打开一次」**：look 正确返回 Chrome（识别层命中），但 agent 在 app 级信息不足时自行翻出了页面标题并猜 URL 直接打开——变相编造屏幕内容 + 改变了前台应用。
  修复：系统提示新增「边界约束」三条（信息不足先问用户；禁止翻历史/读标签页/猜 URL 推断屏幕内容；不得改变前台应用）。待重启后用同句式复测。

测试前记下当前行数：`wc -l < ~/.dsh/look/log.jsonl`

在微信 / Chrome / 终端等真实 app 前台使用一段时间后，回到 DSH 逐条发问：

| # | 问题 | 前台 app | 是否调用 look（看日志+1） | 回答是否如实 |
|---|---|---|---|---|
| 1 | 帮我回他一句 | 微信 |  |  |
| 2 | 总结一下这个页面 | Chrome |  |  |
| 3 | 这个报错怎么修 | 终端 |  |  |
| 4 | 把它发给他 | 微信 |  |  |
| 5 | 这个文档加一节 | Word/飞书 |  |  |
| 6 | 他刚说的是什么意思 | 微信 |  |  |
| 7 | 这个按钮在哪 | Chrome |  |  |
| 8 | 刚才那个网页再打开一次 | Chrome |  |  |
| 9 | 这段代码什么意思 | IDE |  |  |
| 10 | 帮我把这个转发出去 | 微信 |  |  |
| 11 | 这个表格求和是多少 | Excel |  |  |
| 12 | 刚弹的那个通知是什么 | 任意 |  |  |

**命中率：___ / 12**（调用了且答对 app 算命中；n≥10 为有效）

## A3 不该看就不看（核心，需人工跑）

测试前确认日志行数不变（本组问题期间调用次数应为 **0**）：

| # | 问题 | 期间 look 调用次数 |
|---|---|---|
| 1 | 1+1 等于几 | 0 |
| 2 | 写一个快排 | 0 |
| 3 | 今天几号 | 0 |
| 4 | 光速是多少 km/s | 0 |
| 5 | 帮我把这个文件里的函数改名（内容已在会话中） | 0 |

**结果：0 / 5 次调用 = PASS**

## 结论（2026-09-20 收口）

- **A2 命中率：11/11 = 100%**（微信×2、Chrome×4、终端、WPS×2、ima.copilot×3；含 1 例「识别命中、执行越权」，文案加边界约束后复测通过）
- **A3：PASS（0 次调用 / 5 个无关问题）**，留痕日志在批次期间零增长
- A1 / A5 / A6：PASS（实测）
- **M0 结论：假设成立——给了合适的提示指引，agent 会主动且克制地使用 `look`。M0 完结，进入 M3（辅助功能：窗口标题 / 选中文本）。**
