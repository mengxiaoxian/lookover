{
  "name": "dsh-look",
  "version": "0.1.0",
  "description": "M0 scene-awareness probe: a zero-permission `look` tool that reads the frontmost non-self window (app-level info only) on macOS, with system-prompt guidance for when the agent should call it.",
  "type": "module",
  "main": "lib/index.js",
  "files": ["lib", "bin", "native", "cordis.patch.yml", "README.md"],
  "license": "MIT",
  "os": ["darwin"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build:native": "mkdir -p bin && swiftc -O -module-cache-path ./build/mc native/look.swift -o bin/look"
  },
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" }
  }
}
